# Structured Event — External Actions Required (Phase 6)
These are the ONLY remaining steps; all are the user's decisions (the sprint cannot and must not do them).

1. CHOOSE one provider path (budget + priority dependent):
   - cheapest lawful start + already configured: API-Football Pro, or Sportmonks (published EUR tiers);
   - richest (xG/shot-location/positions): Opta or StatsBomb (quote-only, heavier contract);
   - Opta only if FIFA-official WC2026 live/official coverage is required.
2. SEND the outreach packet + questionnaire (notes/.../structured_event_vendor_outreach_packet.md +
   data_requests/pending/structured_event_vendor_questionnaire.*) to the chosen provider(s). NOT yet sent.
3. OBTAIN written confirmation of: local retention, model training on derived features, aggregate research
   publication, raw-redistribution boundary, historical export. (Fail-closed until confirmed.)
4. OBTAIN a sample export (20 historical matches + 5 event timelines + corrections + timing metadata).
5. APPROVE budget / sign the contract or subscribe; place the key in .env OUT OF BAND (never share it with me;
   never commit it).
6. THEN day-one integration begins automatically per structured_event_day_one_integration_plan.md.

Nothing in steps 1-5 can be done inside this sprint (purchase/NDA/quote/credential/legal). On completion of
step 5 the engineering is ready to proceed immediately.
