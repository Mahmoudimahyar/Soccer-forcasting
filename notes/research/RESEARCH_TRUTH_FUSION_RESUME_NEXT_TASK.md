# Resume Next Task
STATUS: INCOMPLETE (tag research-truth-full-corpus-xg-fusion-incomplete). Phase 0-1 DONE (truth registry resolves all conflicting counts; execution manifest 3%). NEXT (multi-day, quota-bounded):
1. Advance AF corpus: python scripts/player_history_resume.py (each day to budget) until execution manifest >=95%.
2. Complete StatsBomb event cache (258 bridge -> >=90% with events).
3. Build the xG-snapshot JOIN (Phase 6) + dynamic snapshot datasets (Phase 4, 30 tests) + full temporal priors (Phase 5).
4. Build rt_jobNN wrappers + run the durable 10h controller; verify ALL Phase-10 hard gates; only then tag complete.
Do NOT tag complete until every gate is objectively true.
