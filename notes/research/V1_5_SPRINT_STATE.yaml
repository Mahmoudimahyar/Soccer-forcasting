sprint: "World Cup Predictor V1.5 — Prospective Operations, Durable Collection, Data Acquisition Readiness"
branch: v1-5-prospective-operations
updated_utc: "2026-06-23"
current_phase: "Phase 1 complete -> Phase 2 (durable collector)"

completed_tasks:
  - "Phase 0: reconcile state"
  - "Phase 1: API-Football direct diagnosis (compatible) + read-only rate-limited adapter + 6 tests"
active_task: "Phase 2: durable prospective collector"
next_task: "Phase 2: durable prospective collector (pre-match/in-play/post-match, idempotent, quota-aware)"

blocked_tasks:
  - id: player_next_goal_card_thresholds
    needs: "paid event provider (StatsBomb commercial / Opta) — StatsBomb open men's-intl ceiling ~333 < 500; reds 50 < 150"
  - id: club_to_international_transfer
    needs: "club rating (Elo-equivalent) — elo_history is national-team only"
  - id: prospective_2026_scores
    needs: "future 2026 match results (time-gated; cannot be produced now)"
  - id: dev_fold_odds
    needs: "timestamp-valid pre-2020 WC odds (unavailable/open-licensed)"

external_actions_required:
  - "Decide on a paid event provider for player/next-goal/card thresholds (Phase 5 decision package)."
  - "Manually install the scheduler when ready (scripts prepared, not auto-installed)."

collector_readiness: "adapter ready; collector in Phase 2"
api_readiness: "DIRECT API-Football compatible (auth accepted; 2026 fixtures/standings/events/subs/cards/lineups all 2xx); read-only adapter built"
frozen_model_version: "m2_frozen (M2_RemainingPoisson) v2 — research_only, parameter-free"
future_fixture_pool: "30 not-started 2026 WC matches (NS) + knockouts to be scheduled; 41 finished are exploratory/seen"
last_successful_test_command: "python -m pytest -q  # 185 passed"
last_stable_commit: "e586a05"

stop_reason: null  # sprint in progress
