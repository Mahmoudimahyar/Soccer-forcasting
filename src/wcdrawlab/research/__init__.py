"""Fixed evaluator + agent-editable candidate model sandbox.

Only `candidate.py` is intended to be modified by Claude Code during autoresearch.
The evaluator, leakage guard, fold definitions, execution layer, and risk policy are
locked from autonomous modification.
"""
